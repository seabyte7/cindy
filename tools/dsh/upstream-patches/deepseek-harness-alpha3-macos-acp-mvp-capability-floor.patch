diff --git a/packages/bundle/acp-app/cordis.patch.yml b/packages/bundle/acp-app/cordis.patch.yml
index c1244f3..0a66dac 100644
--- a/packages/bundle/acp-app/cordis.patch.yml
+++ b/packages/bundle/acp-app/cordis.patch.yml
@@ -8,6 +8,34 @@
 - id: session-title-llm
   disabled: true
-
+
+# Cindy's signed macOS ACP MVP owns only persistent session control. The
+# general dsh-base composition also brings attachment conversion, local
+# subprocess execution and an inner Seatbelt provider. Those rows either load
+# dynamic native add-ons outside the sealed runtime or expose capabilities that
+# the MVP does not yet implement. Keep their dependent tool rows unavailable
+# until each capability has a signed runtime closure and an explicit Cindy
+# bridge contract.
+- id: attachment-local
+  disabled: true
+
+- id: subprocess
+  disabled: true
+
+- id: sandbox
+  disabled: true
+
+- id: bash-sandbox
+  disabled: true
+
+- id: permission
+  disabled: true
+
+- id: tool-bash
+  disabled: true
+
+- id: tool-fs-search
+  disabled: true
+
 - insert:
     - id: acp-app-startup
       name: '@deepseek-ai/dsh-acp-app'
